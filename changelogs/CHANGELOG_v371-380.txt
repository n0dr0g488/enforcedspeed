v371 (2026-02-17) — Static Maps: force server key (GOOGLE_MAPS_SERVER_KEY) for ticket + minimap proxies so web key can disable Static Maps API.

v373 (2026-02-18) — Static Maps: restore custom pin icons in server proxy (regression fix).
v374 (2026-02-18) — Static Maps: fix marker query encoding so Google can fetch custom pin icons (avoid "icon%3A" / default pins + g.co/staticmaperror).

v375 (2026-02-18) — Static Maps: simplify county outline polyline when extremely large to avoid g.co/staticmaperror while preserving required outline.


v376 (2026-02-18) — Static Maps: support Cloudflare R2 static pin host via STATIC_PIN_BASE_URL (e.g. https://static.enforcedspeed.com/pins) for reliable Google icon fetches.


v377 (2026-02-18) — Static Maps: home minimap proxy now uses STATIC_PIN_BASE_URL pin icon (R2) to avoid default pins + g.co/staticmaperror.

v378 (2026-02-18)
- Render deploy reliability: bind gunicorn to $PORT via render.yaml startCommand (prevents port-scan hang / spinning deploy).
